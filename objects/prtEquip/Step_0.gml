/// @description Execute at every. To be inherited
ok = false;
if count == 0 && !using {
    return 0;
}

//Will execute whatever you want every step if you have at least 1 unit of this item

//show_debug_message("Executing step for " + name);

